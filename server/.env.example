# ============================================================
# Tirth Sutra server environment variables
# Copy this file to server/.env and replace placeholders.
# Never commit real secrets.
# ============================================================

# Database
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/tirthsutra?retryWrites=true&w=majority
MONGODB_MAX_POOL_SIZE=10
MONGODB_MIN_POOL_SIZE=1
MONGODB_SERVER_SELECTION_TIMEOUT_MS=10000

# Auth
JWT_SECRET=CHANGE_ME_use_a_long_random_string_at_least_32_chars
AUTH_TOKEN_TTL=30d
JWT_ISSUER=tirth-sutra
JWT_AUDIENCE=tirth-sutra-web
# Local development values:
CLIENT_URL=http://localhost:5000
FRONTEND_URL=http://localhost:5000
SERVER_URL=http://localhost:5000
# Production example:
# CLIENT_URL=https://social-media-coral-tau.vercel.app
# FRONTEND_URL=https://social-media-coral-tau.vercel.app
# SERVER_URL=https://your-backend-domain.com
# Never keep these set to localhost in production.

# Google OAuth
GOOGLE_CLIENT_ID=your_google_web_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Add these in Google Cloud Console:
# Authorized redirect URIs:
#   http://localhost:5000/api/auth/google/callback
#   https://your-backend-domain.com/api/auth/google/callback

# Appwrite OAuth bridge
# The frontend starts Google OAuth in Appwrite, then the backend verifies the Appwrite JWT.
APPWRITE_ENDPOINT=https://nyc.cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=69ea1b4e000d4e4e2b20

# Redis realtime (optional, required for true multi-instance Socket.IO + shared presence)
# Use either REDIS_URL or the host/port fields below.
# Examples:
# REDIS_URL=rediss://default:your_password@your-redis-host:6379
# REDIS_URL=redis://default:your_password@your-plain-redis-host:6379
# REDIS_HOST=your-redis-host
# REDIS_PORT=6379
# REDIS_USERNAME=default
# REDIS_PASSWORD=your_redis_password
# REDIS_DB=0
# REDIS_TLS=true
# Important:
# - Use rediss:// or REDIS_TLS=true only when your Redis provider gives you a TLS-enabled port.
# - Use redis:// or REDIS_TLS=false for plain Redis ports.
# - If you see "SSL routines:tls_get_more_records:packet length too long", your TLS setting does not match the Redis port.
REDIS_ENABLED=false
# REDIS_URL=rediss://default:your_redis_password@your-redis-host:6379
# REDIS_URL=redis://default:your_redis_password@your-plain-redis-host:6379
# REDIS_HOST=your-redis-host
# REDIS_PORT=6379
# REDIS_USERNAME=default
# REDIS_PASSWORD=your_redis_password
# REDIS_DB=0
# REDIS_TLS=true
REDIS_KEY_PREFIX=tirthsutra
REDIS_PRESENCE_TTL_SECONDS=90
# Optional MongoDB read caching through the same Redis instance.
REDIS_CACHE_ENABLED=true
REDIS_CACHE_DEFAULT_TTL_SECONDS=120
# Optional override. Defaults to about one-third of REDIS_PRESENCE_TTL_SECONDS.
# REDIS_PRESENCE_HEARTBEAT_MS=30000

# SMTP / Nodemailer
# Gmail example for Tirth Sutra OTP signup emails:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tirthsutra@gmail.com
# SMTP_PASS=your_google_app_password
# EMAIL_FROM=tirthsutra@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tirthsutra@gmail.com
SMTP_PASS=your_google_app_password
EMAIL_FROM=tirthsutra@gmail.com
SMTP_VERIFY_ON_STARTUP=false
SMTP_VERIFY_BEFORE_SEND=false

# 1-year email drip campaign
# Sends only to users who tick the signup marketing checkbox.
EMAIL_CAMPAIGN_ENABLED=true
EMAIL_CAMPAIGN_PUBLIC_URL=https://your-backend-domain.com
EMAIL_CAMPAIGN_CLIENT_URL=https://your-frontend-domain.com
EMAIL_CAMPAIGN_DEFAULT_CTA_URL=https://shaleen17.github.io/Tirth-Sutra/
EMAIL_CAMPAIGN_SOCIAL_URL=https://shaleen17.github.io/Tirth-Sutra/
EMAIL_CAMPAIGN_MISSION_URL=https://shaleen17.github.io/Tirth-Sutra/
EMAIL_CAMPAIGN_SHOP_URL=https://shaleen17.github.io/Tirth-Sutra/
EMAIL_CAMPAIGN_BATCH_SIZE=25
EMAIL_CAMPAIGN_WORKER_INTERVAL_MS=300000
EMAIL_CAMPAIGN_START_DELAY_HOURS=24
EMAIL_CAMPAIGN_SEND_WINDOW_START=9
EMAIL_CAMPAIGN_SEND_WINDOW_END=18
EMAIL_CAMPAIGN_CRON_SECRET=CHANGE_ME_for_cron_endpoint
EMAIL_CAMPAIGN_ADMIN_KEY=CHANGE_ME_for_stats_endpoint

# Backward-compatible aliases if you prefer the old names
EMAIL_USER=tirthsutra@gmail.com
EMAIL_PASS=your_google_app_password

# Cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Razorpay
# Use test keys in development and live keys in production only after activation.
RAZORPAY_KEY_ID=rzp_test_your_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Web Push (optional but recommended for stable browser subscriptions)
# Generate once and keep them fixed in production.
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
VAPID_SUBJECT=mailto:your_email@example.com

# App
PORT=5000
NODE_ENV=development
JSON_BODY_LIMIT=10mb
URLENCODED_BODY_LIMIT=2mb

# Security / abuse protection
# In production, set FRONTEND_URL and CLIENT_URL to your real website domain.
RATE_LIMIT_API_MAX=900
RATE_LIMIT_API_WINDOW_MS=900000
RATE_LIMIT_AUTH_MAX=80
RATE_LIMIT_AUTH_WINDOW_MS=900000
RATE_LIMIT_WRITE_MAX=180
RATE_LIMIT_WRITE_WINDOW_MS=600000
RATE_LIMIT_UPLOAD_MAX=40
RATE_LIMIT_UPLOAD_WINDOW_MS=3600000

# Upload limits
UPLOAD_MAX_BYTES=26214400
UPLOAD_IMAGE_MAX_BYTES=5242880
UPLOAD_VIDEO_MAX_BYTES=26214400
UPLOAD_AUDIO_MAX_BYTES=10485760
UPLOAD_DOCUMENT_MAX_BYTES=5242880
UPLOAD_BASE64_MAX_BYTES=8388608

# Monitoring / admin endpoints
# GET /api/admin/monitoring with header x-monitoring-key
MONITORING_ADMIN_KEY=CHANGE_ME_for_monitoring
# POST /api/admin/backup and GET /api/admin/backup/status with header x-backup-key
BACKUP_ADMIN_KEY=CHANGE_ME_for_backups

# Database backups
DB_BACKUP_ENABLED=false
DB_BACKUP_INTERVAL_HOURS=24
DB_BACKUP_RETENTION=10
# DB_BACKUP_DIR=/persistent/backups

# Logging
FILE_LOGS_ENABLED=true
SLOW_REQUEST_MS=1200

# MyMemory Translation (used by the website language switcher)
# FREE — no installation, no API key required. Works on Render, Vercel, Railway etc.
# Optional: set your email below to raise the free daily limit from 5k to 10k words.
# Sign up (free) at https://mymemory.translated.net to get an email-linked quota.
# MYMEMORY_EMAIL=your_email@example.com

# Support inbox
SUPPORT_EMAIL=tirthsutra@gmail.com
